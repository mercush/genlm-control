[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "genlm-control"
version = "0.0.1"
description = ""
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "The GenLM Team" },
]
dependencies = [
    "genlm_grammar @ git+ssh://github.com/chi-collective/genlm-grammar.git",
    "genlm_backend @ git+ssh://github.com/chi-collective/genlm-backend.git",
    "hfppl @ git+ssh://github.com/probcomp/hfppl.git",
    "arsenal @ git+ssh://github.com/timvieira/arsenal.git",
    "IPython",
    "numpy",
    "torch",
]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-benchmark",
    "pytest-asyncio",
]
docs = [
    "mkdocs",
    "mkdocstrings[python]",
    "mkdocs-material",
    "mkdocs-gen-files",
    "mkdocs-literate-nav",
    "mkdocs-section-index",
]
